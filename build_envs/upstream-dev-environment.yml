name: geocat_comp_upstream
channels:
  - conda-forge
dependencies:
  - python>=3.8, <3.11
  - cftime
  - eofs
  - geocat-datafiles  # for tests
  - geocat-f2py
  - netcdf4  # for tests
  - parameterized  # for tests
  - pytest  # for tests
  - pytest-cov  # for tests
