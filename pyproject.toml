[build-system]
build-backend = "setuptools.build_meta"
requires = [
  "setuptools>=42",
  "setuptools-scm>=7",
]

[tool.setuptools_scm]
fallback_version = "999"

[tool.codespell]
ignore-words-list = "alph, ond,"
skip = '*.ipynb'
